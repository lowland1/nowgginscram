services:
  - type: web
    name: nowgginscram
    runtime: static
    buildCommand: |
      git submodule update --init --recursive
      ./scripts/prepare-scramjet-client.sh
    staticPublishPath: .
